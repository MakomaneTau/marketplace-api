import {Router} from "express"; import {authenticate} from "../middleware/authenticate.js"; import {unreadMessageCount,getConversation,listConversations,listNotifications,markAllNotificationsRead,markRead,markNotificationRead,sendMessage,startConversation} from "../controllers/messaging.controller.js";
export const conversationsRouter=Router(); conversationsRouter.use(authenticate); conversationsRouter.get("/",listConversations); conversationsRouter.post("/",startConversation); conversationsRouter.get("/unread-count",unreadMessageCount); conversationsRouter.get("/:id",getConversation); conversationsRouter.post("/:id/messages",sendMessage); conversationsRouter.patch("/:id/read",markRead);
export const notificationsRouter=Router(); notificationsRouter.use(authenticate); notificationsRouter.get("/",listNotifications); notificationsRouter.patch("/read-all",markAllNotificationsRead); notificationsRouter.patch("/:id/read",markNotificationRead);
