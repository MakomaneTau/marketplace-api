import"dotenv/config";import{randomUUID}from"node:crypto";import{supabaseAdmin}from"../src/config/supabase.js";import{getSettings,updateSettings}from"../src/services/settings.service.js";let id;try{const{data,error}=await supabaseAdmin.auth.admin.createUser({email:`settings-${randomUUID()}@example.test`,password:`Smoke-${randomUUID()}!`,email_confirm:true,user_metadata:{role:"seller",firstName:"Settings",lastName:"Smoke",display_name:"Settings Smoke",isStudent:false}});if(error)throw error;id=data.user.id;const initial=await getSettings(id);const updated=await updateSettings(id,{notify_marketplace_tips:true});if(initial.notify_marketplace_tips!==false||updated.notify_marketplace_tips!==true)throw new Error("settings smoke assertion failed");console.log(JSON.stringify({status:"ok",checks:["default preferences","persisted preference update"]}));}finally{if(id)await supabaseAdmin.auth.admin.deleteUser(id);}
